#include<bits/stdc++.h>
// use driver code and  input graph, the diffusion model M ,​ the parameters k, t.
// driver
void kempg(){
    // intialize new member of class bcrim
    
    // call influence function of the new instance
    //  and then dellocate the pointer clearing the memory
}

int main(){
    // path to data nodes
    // define model(LT OR IC)
    // define k
    // define t

    // call kempg for the given model
    return 0;
}